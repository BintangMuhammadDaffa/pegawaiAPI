spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/company_db
    username: postgres
    password: binterex
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always

server:
  port: 8080
